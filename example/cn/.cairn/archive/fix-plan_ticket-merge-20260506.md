# fix-plan_ticket-merge：重复工单合并（归档于 2026-05-06）

- 内容：运营主管提出的重复工单合并能力，属当前 plan 之外的新功能
- 时间：2026-05-06 15:10
- 来源：运营主管在预发环境试用后提出
- 预期影响 plan 范围：当前版本目标（新增一项）、核心对象（新增 Merge Record）、关键流程（新增「工单合并」，修订「备注审计」）、验收标准（新增两条）

## 讨论与确认

### 第一层：用户明确要求

- P-01 同一客户在 24 小时内重复提交的工单，主管可以手动合并为一条主工单。
- P-02 合并后被合并工单进入只读状态，详情页保留跳转到主工单的入口。
- P-03 合并操作写入审计事件。

### 第二层：代理建议（逐条确认）

- P-04 合并提供 10 分钟内可撤销窗口。**用户确认：采纳。**
- P-05 合并时把被合并工单的备注迁移到主工单，保留原始来源标记。**用户确认：采纳，但仅迁移内部与跨团队备注；审计备注不迁移，避免审计轨迹跨工单串写。**
- P-06 自动识别疑似重复工单并提示主管。**用户确认：不采纳，本期只做手动合并，自动识别留待后续评估。**
- P-07 把合并纳入批量操作入口。**用户确认：不采纳，合并需要逐条确认主工单，不适合批量。**

### 最终确认

- 时间：2026-05-06 15:40
- 用户确认语：合并方案定稿，按 P-01 至 P-05 写入 plan。

## 写入 plan.md 的结果

| plan 位置 | 动作 | 对应条目 |
| --- | --- | --- |
| 当前版本目标 | 新增「支持重复工单合并」 | P-01 |
| 核心对象 | 新增 `Merge Record` | P-01、P-02、P-04 |
| 关键流程 | 新增「工单合并」小节 | P-01 至 P-04 |
| 关键流程 / 备注审计 | 修订：补充合并场景下备注迁移的可见性边界 | P-05 |
| 验收标准 | 新增两条（只读与主工单跳转、审计轨迹不跨工单） | P-02、P-03、P-05 |

未采纳的 P-06、P-07 只保留在本文件，不进入 `plan.md`。

## 对已完成 TODO 的影响

- 步骤 7「工单详情和基础备注」已归档于 `archive/done-20260505.md`。合并后详情页需要展示只读态和主工单跳转，属于对已完成范围的扩展。
- 判定：不重新打开步骤 7，改为在其归档条目下追加一行说明，实现工作由新版 `plan.md` 派生的步骤 11 承接。已在 `archive/done-20260505.md` 中完成追加。

## 归档说明

`plan.md` 写入与来源标注已完成，本文件按协议先于实现 TODO 归档。后续实现见 `TODO.md` 步骤 11。
